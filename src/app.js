import main from './scripts/main.js'
import './styles/style.css'

document.addEventListener('DOMContentLoaded', main);

// {
//     main()
//     DataSource.getPokemon().then(renderResult).catch(fallbackResult)
// }
